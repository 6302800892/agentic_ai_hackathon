# Agent framework
langgraph>=0.4
langchain-core>=0.3
langchain-google-genai>=2.1          # Gemini is the ONLY model provider

# Memory (short-term checkpoints + long-term store) + LangMem extraction
langgraph-checkpoint-sqlite>=2.0.10
aiosqlite>=0.20
langmem>=0.0.25

# MCP
mcp>=1.9
langchain-mcp-adapters>=0.1

# Retrieval
chromadb>=0.6
sentence-transformers>=3.0

# Observability (Phoenix, in-process) + OpenTelemetry
arize-phoenix>=8.0
arize-phoenix-otel>=0.9
openinference-instrumentation-langchain>=0.1.30
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-http>=1.25
httpx>=0.27                          # Phoenix REST span export

# Evaluation + tests
deepeval>=3.0                        # 2.x is incompatible with langchain 1.x
pytest>=8.0
pytest-asyncio>=0.23

# Security
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
python-dotenv>=1.0

# Utilities
pydantic>=2.7
pyyaml>=6.0
typer>=0.12
tenacity>=8.2
pandas>=2.2
pyarrow>=15.0
matplotlib>=3.8

# Bonus: FastAPI streaming
fastapi>=0.110
uvicorn>=0.29
sse-starlette>=2.1
